# Copy to `.env` for local dev (vercel dev), or set in your host's dashboard.
# Slack → your app → Incoming Webhooks → copy the webhook for the target channel.
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
